prototype(Jonnitto.PrettyEmbedHelper:Notification) < prototype(Carbon.Notification:Backend) {
    key = null
    type = 'alert'
    package = ${package}
    default = null

    content = ${Translation.translate(this.key, this.default, [], null, 'Jonnitto.PrettyEmbed' + this.package)}
}
